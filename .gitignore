# Binaries
/agentkit
/agentdemo
# `go build ./examples/<name>` drops the binary in the working directory.
/chat
/streaming
/codingagent
/session
# The line above also matches the session/ package directory for files git
# does not yet track; this re-includes the directory so a new file there is
# not silently ignored.
!/session/
/delegation
/flatline
/examples/flatline/flatline
# `make build-examples` drops binaries here.
/bin/

# Test artifacts
*.test
*.out
coverage.*

# Fuzz corpora (seed corpora under testdata/ are committed)
testdata/fuzz/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

.nightshift/
prompts.md

